{
    "server": {
        "name": "localhost",
        "listen": "*",
        "port": 8000,
        "document_root": "WROOT_DOCUMENT_ROOT",
        "compressed_output": true
    },
    "url_rewrite": [
        {
            "input": "^(.+)$",
            "output": "$1"
        }
    ]
}